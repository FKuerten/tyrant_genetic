load_makefile modules/binary

target/tyrantGenetic: modules/binary/target/tyrantGenetic
    @mkdir -p target
    cp ${input} ${output}

target/dependencies: modules/binary/target/dependencies
    @mkdir -p target
    cp ${input} ${output}

$(phony compile): modules/binary/binaries

$(phony list_libraries): target/dependencies
    cat $(input)

tyrant_optimize: ../tyrant_optimize/tyrant_optimize
    cp ${input} ${output}
